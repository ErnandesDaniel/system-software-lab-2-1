// Пример функции как значения первого класса
// Функциональный литерал присваивается переменной и вызывается через неё

extern puts

def main() of int
    puts("Testing function literal:");

    f = def square(x of int) of int
        return x * x
    end

    return f(5)   // 25
end
