// Пример локальной функции (nested def)
// Функция double определена внутри main и видна только там

extern puts

def main() of int
    def double(x of int) of int
        return x * 2
    end

    puts("Testing local function:");
    return double(21)
end
