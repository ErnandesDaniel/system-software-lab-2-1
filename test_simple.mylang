extern fopen
extern fclose
extern fgetc
extern puts
extern str_set_byte

global buf of string = "                                                                                                                                                                                                                                ";

def read_line(dst of string, f of string) of int
    return 0
end

def main() of int
    puts("opening")
    f = fopen("labs-examples/system-programms/lab-2/csv-data/people.csv", "r")
    if f != "" then
        read_line(buf, f)
        fclose(f)
    end
    puts("done")
    return 0
end
