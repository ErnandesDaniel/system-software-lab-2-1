extern fopen
extern fclose
extern fgetc
extern puts

def main() of int
    puts("start")
    f = fopen("labs-examples/system-programms/lab-2/csv-data/people.csv", "r")
    if f != "" then
        c = fgetc(f)
        puts("read ok")
        fclose(f)
    end
    puts("done")
    return 0
end
